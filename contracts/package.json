{
  "name": "@wafflefinance/contracts",
  "version": "1.0.0",
  "description": "Ethereum smart contracts for WaffleFinance cross-chain token swaps",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:validate": "hardhat test test/validateEnv.test.ts",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network mainnet",
    "deploy:phase6": "hardhat run scripts/deploy-phase6.ts --network hardhat",
    "deploy:sepolia:phase6": "hardhat run scripts/deploy-sepolia-phase6.ts --network sepolia",
    "verify": "hardhat verify",
    "clean": "hardhat clean && rm -rf artifacts cache",
    "lint": "solhint 'contracts/**/*.sol'",
    "coverage": "hardhat coverage"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^1.1.0",
    "@typechain/hardhat": "^9.1.0",
    "@types/chai": "^4.3.0",
    "chai": "^4.2.0",
    "hardhat": "^2.19.0",
    "hardhat-gas-reporter": "^1.0.9",
    "solhint": "^4.1.0",
    "solidity-coverage": "^0.8.5"
  },
  "keywords": [
    "ethereum",
    "solidity",
    "smart-contracts",
    "htlc",
    "cross-chain",
    "defi"
  ]
}